<% if (includes.length) { for (var inc of includes) { %>
import {<%- inc %>} from "./<%- inc %>";<% } } %>

<% if(doc) { %>
/**
* <%- doc %>
*/
<% } %>
export interface <%- name %> <% if (extension) { %>extends <%- extension %><% } %> {
<% for (let i = 0; i < fields.length; i++ ) { %>
    <%if (fields[i].doc){%>
    /**
    * <%- fields[i].doc %>
    */<%}%>
    <%- fields[i].name %><% if(fields[i].isOptional === true) { %>?<% } %>: <%- fields[i].type %>;
<% } %>
}